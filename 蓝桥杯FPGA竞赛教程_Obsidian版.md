---
title: 蓝桥杯 FPGA 竞赛教程
aliases:
  - 蓝桥杯FPGA教程
  - FPGA赛场手册
  - FPGA零基础教程
author: Aix，极道工作室
version: v0.3.1
status: publish
tags:
  - FPGA
  - 蓝桥杯
  - Verilog
  - 竞赛
  - 赛场手册
  - Obsidian
---

# 蓝桥杯 FPGA 竞赛教程

作者：Aix，极道工作室  
定位：零基础教学教程 · 赛场纸质速查手册 · 出版预备稿 · Obsidian 单文件版  
对应 PDF：`results/蓝桥杯FPGA开发教程_详细注释版.pdf`
真题复盘索引：[[蓝桥杯FPGA真题复盘_Obsidian索引]]

> [!NOTE] 使用说明
> 这个文件是给 Obsidian 阅读、搜索、批注和复习用的 Markdown 教程版。完整排版、扫描题面图片、页脚总页数、PDF 书签和纸质装订效果以 PDF 为准。

## 目录

- [[#1 使用方法]]
- [[#2 FPGA 最小知识框架]]
- [[#3 I2C 协议]]
- [[#4 UART 协议]]
- [[#5 SPI 协议]]
- [[#6 状态机设计]]
- [[#7 按键消抖]]
- [[#8 亚稳态与跨时钟域]]
- [[#9 数码管动态扫描]]
- [[#10 SRAM 读写时序]]
- [[#11 DS1302 RTC]]
- [[#12 W25Q128 Flash]]
- [[#13 驱动模块学习路线]]
- [[#14 真题与模拟题使用方式]]
- [[#15 赛场调试顺序]]
- [[#16 出版与纸质版注意事项]]
- [[#17 最小复习清单]]
- [[#18 Obsidian 题目复盘模板]]
- [[#19 Obsidian 标签体系]]
- [[#20 高频检索关键词]]
- [[#21 Obsidian Vault 推荐结构]]

## 1 使用方法

如果你是零基础读者，不要一开始就背代码。蓝桥杯 FPGA 题目的核心不是“写很多 Verilog”，而是把硬件需求拆成稳定的时序模块，再用顶层模块把它们连接起来。

推荐学习顺序：

1. 先读硬件平台：弄清楚时钟、复位、按键、LED、数码管、I2C、SPI、UART、SRAM、RTC、Flash 分别接到哪些引脚。
2. 再读通用方法：掌握计数器、边沿检测、分频、状态机、同步器和消抖。
3. 然后读驱动模块：每个外设只关心接口、时序、开始信号、完成信号和错误点。
4. 最后做真题：先画模块框图，再写顶层连线，最后上板验证。

> [!TIP] Obsidian 用法
> 建议把本文件放进一个单独 vault，给重点段落加 `#易错点`、`#待复习`、`#已掌握`。做题时可以在每道题下方追加自己的调试记录。

> [!WARNING] 不要只看代码
> FPGA 竞赛不是把模板复制完就结束。你必须知道每个模块什么时候启动、什么时候完成、错误时能观察哪个信号。

## 2 FPGA 最小知识框架

FPGA 程序本质上是在描述电路，不是在描述 CPU 顺序执行的程序。`always @(posedge clk)` 表示一组触发器，`assign` 表示组合逻辑连线。初学者最常见的问题，是把 Verilog 当 C 语言写，导致时序混乱、信号延迟一拍、状态跳转不可控。

必须先建立三个概念：

- 时钟：所有寄存器在时钟边沿统一更新。
- 复位：让状态机、计数器、输出寄存器回到确定初值。
- 使能：控制某个模块在某一拍开始工作，而不是让它每拍都重复启动。

推荐每个模块都遵守这个接口习惯：

```verilog
input  wire clk;
input  wire rst_n;
input  wire start;
output reg  done;
output reg  busy;
```

这样顶层模块只需要用 `start/busy/done` 管理任务，不需要深入关心每个外设内部的细节。

> [!EXAMPLE] 顶层思路
> 题目功能 = 输入采集 + 任务控制状态机 + 外设驱动 + 显示/通信输出。先画这四块，再写代码。

## 3 I2C 协议

I2C 使用两根线：`SCL` 是时钟线，`SDA` 是数据线。它是开漏总线，空闲时由上拉电阻拉高。主机负责产生 SCL，并在 SDA 上发送地址、读写方向和数据。

起始条件是：`SCL` 为高电平时，`SDA` 从高变低。停止条件是：`SCL` 为高电平时，`SDA` 从低变高。因为普通数据变化通常要求发生在 `SCL` 低电平期间，所以这两个特殊变化能被从机识别为帧边界。

一次典型写操作包含：

1. START
2. 7 位器件地址 + 1 位写方向
3. 等待 ACK
4. 寄存器地址
5. 等待 ACK
6. 数据字节
7. 等待 ACK
8. STOP

ACK 位由接收方拉低 SDA 表示。如果 ACK 没有出现，通常说明器件地址错误、连线错误、时序太快，或者从机没有正常供电。

> [!TIP] 赛场写法
> 把一个字节发送拆成 8 个 bit 状态，再单独做 ACK 状态。不要把“发地址、发寄存器、发数据”全部写在一个巨大状态里。

## 4 UART 协议

UART 是异步串口，不需要共享时钟。收发双方约定波特率，例如 9600、115200。最常见帧格式是：

- 1 位起始位：低电平。
- 8 位数据位：通常低位先发。
- 可选校验位：奇校验或偶校验。
- 1 位停止位：高电平。

波特率计数器的计算方法：

```text
计数值 = 系统时钟频率 / 目标波特率
```

例如 50 MHz 时钟、115200 波特率，理论计数值约为 434。接收时不要在起始位边沿立刻采样数据，而应等待半个 bit 时间到达中点，再每隔一个 bit 时间采样一次，这样抗抖动能力更好。

> [!NOTE] 调试价值
> UART 很适合输出状态机状态、错误码、采样值和外设完成标志。赛场上能打印一个状态字节，排错速度会明显提升。

## 5 SPI 协议

SPI 常见四根线：

- `SCLK`：主机产生的串行时钟。
- `MOSI`：主机输出，从机输入。
- `MISO`：从机输出，主机输入。
- `CS_N`：片选，通常低有效。

SPI 是全双工协议：主机每发出 1 bit，同时也会从 MISO 采回 1 bit。很多初学者只关注“发送命令”，忽略“接收数据也在同一串时钟里发生”，这会导致读 Flash、读 ADC 或读寄存器时错位。

CPOL/CPHA 决定时钟空闲电平和采样边沿：

- CPOL=0：SCLK 空闲为低。
- CPOL=1：SCLK 空闲为高。
- CPHA=0：第一个有效边沿采样。
- CPHA=1：第二个有效边沿采样。

> [!WARNING] 易错点
> SPI 读操作不是“先发完命令再凭空得到数据”，而是继续给时钟，让从机在后续 bit 中把数据移出来。

## 6 状态机设计

蓝桥杯 FPGA 大题几乎都可以拆成状态机。推荐三段式状态机：

1. 第一段：时序逻辑保存当前状态。
2. 第二段：组合逻辑计算下一状态。
3. 第三段：时序逻辑输出控制信号。

Moore 状态机的输出只由当前状态决定，稳定、好调试。Mealy 状态机的输出由当前状态和输入共同决定，响应快，但更容易受输入毛刺影响。赛场上除非明确需要立即响应，优先用 Moore 风格。

```verilog
localparam IDLE  = 4'd0;
localparam START = 4'd1;
localparam SEND  = 4'd2;
localparam WAIT  = 4'd3;
localparam DONE  = 4'd4;
```

> [!TIP] 命名原则
> 不要用 `S0/S1/S2` 这种只对作者本人有意义的名字。出版稿和赛场复盘都需要让状态含义一眼可见。

## 7 按键消抖

机械按键按下和释放时，触点会在几毫秒内反复接通、断开。如果不消抖，FPGA 会把一次按键识别成多次触发。

常用方法是 20 ms 延时确认：

1. 检测到按键电平变化。
2. 启动计数器。
3. 计数到约 20 ms 后再次读取按键。
4. 如果电平仍保持变化后的状态，则确认按键有效。

按键输入来自 FPGA 外部，必须先经过两级同步器再进入消抖逻辑。否则按键边沿可能撞上时钟采样边沿，引发亚稳态。

## 8 亚稳态与跨时钟域

亚稳态是触发器在建立时间或保持时间不满足时，输出短时间无法稳定到 0 或 1 的现象。它不能被彻底消除，只能降低传播概率。

对于单 bit 异步输入，最常用方法是两级同步器：

```verilog
reg sync_0, sync_1;
always @(posedge clk or negedge rst_n) begin
    if (!rst_n) begin
        sync_0 <= 1'b0;
        sync_1 <= 1'b0;
    end else begin
        sync_0 <= async_in;
        sync_1 <= sync_0;
    end
end
```

> [!WARNING] 多 bit 总线
> 多 bit 数据不能简单逐位同步，否则不同 bit 可能在不同拍稳定。多 bit 跨时钟域应使用握手、异步 FIFO 或灰码计数器。

## 9 数码管动态扫描

多位数码管通常共用段选线，单独控制位选线。FPGA 以很快速度轮流点亮每一位，人眼因为视觉暂留会看到所有位同时亮。

关键点：

- 扫描频率太低会闪烁。
- 扫描频率太高会降低单个位的有效亮度。
- 切换位选前最好先关闭段选，避免重影。
- 段码表要区分共阳和共阴。

推荐拆成三个模块：

1. 数值转 BCD。
2. BCD 转段码。
3. 动态扫描。

不要把计算、译码和扫描全塞进一个 always 块。

## 10 SRAM 读写时序

SRAM 看起来像“给地址就有数据”，但真实硬件必须满足地址建立时间、数据建立时间、写使能宽度和保持时间。

写操作通常流程：

1. 给出地址。
2. 给出待写数据。
3. 片选有效、写使能有效。
4. 保持足够时间。
5. 释放写使能。

读操作通常流程：

1. 给出地址。
2. 片选有效、读使能有效。
3. 等待数据稳定。
4. 采样数据。

> [!TIP] 实现建议
> FPGA 里建议用状态机插入等待拍，不要在同一拍改地址又立即读取数据。

## 11 DS1302 RTC

DS1302 是实时时钟芯片，常见接口类似 3 线 SPI：`CE`、`SCLK`、`IO`。它的数据线是双向的，写时 FPGA 驱动 IO，读时 FPGA 必须释放 IO，让芯片驱动数据。

DS1302 常见操作：

- 写控制寄存器，关闭写保护。
- 写秒、分、时、日、月、星期、年。
- 读当前时间。
- 使用突发读写一次传输多个时间寄存器。

> [!WARNING] BCD 编码
> 十进制 59 在 BCD 中是 `8'h59`，不是二进制 59。显示到数码管前要按十位和个位拆分。

## 12 W25Q128 Flash

W25Q128 是 SPI Flash，常见命令包括：

- `0x9F`：读取 JEDEC ID。
- `0x06`：写使能。
- `0x03`：普通读数据。
- `0x02`：页编程。
- `0x20`：扇区擦除。
- `0x05`：读状态寄存器。

Flash 写入前必须先写使能。页编程通常不能跨页，扇区擦除比页编程慢得多。写或擦除后必须轮询忙标志，不能立刻继续下一条写命令。

> [!TIP] 先读 ID
> 赛场建议先实现读 ID。只要 ID 能读对，说明 SPI 模式、片选、时钟和 MISO 基本正常，再继续做读写数据。

## 13 驱动模块学习路线

阅读 driver 和 user 源码时，不要从第一行开始硬看。推荐按以下问题读：

1. 这个模块解决哪个硬件问题？
2. 输入输出信号分别代表什么？
3. `start` 或触发条件在哪里？
4. `busy/done` 或完成标志在哪里？
5. 状态机有几个状态？每个状态做什么？
6. 计数器对应真实时间还是 bit 位置？
7. 顶层模块如何连接它？

每个驱动都要整理成“用途、接口、时序、易错点、调试信号”五栏。这样考试时可以快速复用，不需要现场重新理解。

## 14 真题与模拟题使用方式

当前教材覆盖以下题面资源：

- 第十六届蓝桥杯电子类 FPGA 设计与开发省赛真题。
- 第十六届蓝桥杯 FPGA 模拟试题 I。
- 第十六届蓝桥杯 FPGA 模拟试题 II。
- 第十六届蓝桥杯 FPGA 模拟试题 III。
- 第十六届蓝桥杯 FPGA 国赛题。
- 第十七届 FPGA 模拟考试 I。
- 第十七届 FPGA 模拟考试 II。

> [!NOTE] 题面说明
> Markdown 文件只做题目索引和解题方法说明；完整扫描题面以 PDF 中的嵌入页面为准。

做题流程：

1. 用 5 分钟标出输入、输出、外设和评分点。
2. 用 10 分钟画顶层模块框图。
3. 先写能上板看到现象的最小版本。
4. 再补完整功能和异常处理。
5. 最后保留调试信号，不要为了整洁删掉所有观察点。

## 15 赛场调试顺序

遇到上板不工作时，不要立刻大改代码。按下面顺序查：

1. 时钟是否正确。
2. 复位极性是否正确。
3. 引脚约束是否匹配赛场板卡。
4. 按键是否经过同步和消抖。
5. 状态机是否离开 IDLE。
6. 外设片选和时钟是否有波形。
7. `done` 信号是否出现。
8. 显示模块是否拿到了正确数据。

如果有 UART，优先打印状态码。如果有 LED，至少保留几个调试灯：心跳灯、状态机低位、错误标志、外设完成标志。

## 16 出版与纸质版注意事项

纸质书和普通电子文档不同，读者无法随时搜索，所以必须强化页脚、目录、表格、索引和检查清单。v0.3.1 已将页脚改为“当前页 / 总页数”，适合装订后检查缺页。

后续出版稿建议继续完善：

- 把每道扫描题转写成结构化文字。
- 每道题增加模块框图、评分点和常见错误。
- 给每个外设增加一页“赛场最小可用模板”。
- 给附录增加更完整的引脚表、段码表、错误排查表。

## 17 最小复习清单

考前至少确认自己能独立完成：

- 写一个同步复位计数器。
- 写一个按键消抖模块。
- 写一个三段式状态机。
- 写一个数码管动态扫描模块。
- 用 I2C 写一个寄存器。
- 用 SPI 读一个器件 ID。
- 用 UART 发送一个状态字节。
- 按题目要求连接顶层模块和约束文件。

> [!SUCCESS] 目标
> 真正的竞赛能力不是背完所有代码，而是看到题目后能快速判断“这题需要哪些模块、模块之间怎么握手、上板后先看哪个信号”。

## 18 Obsidian 题目复盘模板

把下面模板复制到每一道真题或模拟题后面，做完一题就填一次。复盘比单纯刷题更重要，因为 FPGA 错误通常不是“不会写”，而是“没有定位到哪个时序环节错了”。

```markdown
## 题目复盘

- 题目来源：
- 完成日期：
- 用时：
- 是否上板成功：
- 最终得分估计：

### 需求拆解

- 输入信号：
- 输出信号：
- 使用外设：
- 必须完成的评分点：
- 可选优化点：

### 模块划分

- 顶层控制：
- 按键/输入：
- 显示输出：
- 通信/存储：
- 调试信号：

### 本题状态机

- IDLE：
- INIT：
- WORK：
- WAIT：
- DONE：
- ERROR：

### 易错点

- [ ] 复位极性是否正确
- [ ] 引脚约束是否匹配
- [ ] 按键是否消抖
- [ ] 跨时钟信号是否同步
- [ ] 外设 busy/done 是否处理
- [ ] 显示数据是否锁存

### 下次改进

-
```

> [!TIP] 复盘原则
> 每次只记录能指导下次上板的结论。不要写“继续努力”这种无效文字，要写“SPI 读操作需要继续给 24 个 dummy clock”这种可执行经验。

## 19 Obsidian 标签体系

建议在 Obsidian 里使用稳定标签，后续检索和复习会更快：

- `#FPGA/协议/I2C`
- `#FPGA/协议/SPI`
- `#FPGA/协议/UART`
- `#FPGA/外设/数码管`
- `#FPGA/外设/SRAM`
- `#FPGA/外设/DS1302`
- `#FPGA/外设/W25Q128`
- `#FPGA/方法/状态机`
- `#FPGA/方法/消抖`
- `#FPGA/方法/跨时钟域`
- `#蓝桥杯/真题`
- `#蓝桥杯/模拟题`
- `#赛场/易错点`
- `#赛场/上板调试`
- `#待复习`
- `#已掌握`

> [!NOTE] 标签习惯
> 标签不要太多，也不要每次临时发明。固定标签的价值在于半年后还能搜到同一类问题。

## 20 高频检索关键词

在 Obsidian 中可以直接搜索这些关键词定位知识点：

| 目标 | 搜索关键词 |
|---|---|
| I2C 起始与停止 | `START`、`STOP`、`ACK`、`SDA`、`SCL` |
| UART 波特率 | `波特率`、`起始位`、`停止位`、`采样` |
| SPI 模式 | `CPOL`、`CPHA`、`MOSI`、`MISO`、`CS_N` |
| 状态机 | `三段式`、`Moore`、`Mealy`、`IDLE`、`DONE` |
| 按键 | `消抖`、`20 ms`、`边沿检测` |
| 跨时钟域 | `亚稳态`、`两级同步器`、`CDC` |
| 数码管 | `动态扫描`、`段选`、`位选`、`视觉暂留` |
| SRAM | `地址建立`、`保持时间`、`写使能` |
| DS1302 | `BCD`、`突发读写`、`写保护` |
| W25Q128 | `写使能`、`页编程`、`扇区擦除`、`JEDEC ID` |

> [!SUCCESS] 使用目标
> 这份 Obsidian 文件不是替代 PDF，而是用来搜索、批注、复盘和形成个人知识库。PDF 负责纸质阅读和完整题面，Obsidian 负责长期积累。

## 21 Obsidian Vault 推荐结构

建议把教程、PDF、真题复盘放在同一个 vault 中，后续搜索和回链会更自然。

```text
蓝桥杯FPGA/
├── 蓝桥杯FPGA竞赛教程_Obsidian版.md
├── results/
│   └── 蓝桥杯FPGA开发教程_详细注释版.pdf
├── 真题复盘/
│   ├── 16届省赛.md
│   ├── 16届模拟I.md
│   ├── 16届模拟II.md
│   ├── 16届模拟III.md
│   ├── 17届模拟I.md
│   └── 17届模拟II.md
└── 上板记录/
    ├── 数码管动态扫描.md
    ├── SPI_Flash.md
    └── DS1302_RTC.md
```

如果 PDF 也放进 vault，可以在任意笔记中嵌入：

```markdown
![[results/蓝桥杯FPGA开发教程_详细注释版.pdf]]
```

建议新建三个常用入口笔记：

- `今日复习.md`：记录今天要看的协议、外设和错题。
- `上板问题汇总.md`：只记录真实上板遇到的问题和解决方法。
- `考前清单.md`：只放最后 24 小时要看的内容。

> [!TIP] 长期维护方法
> PDF 是稳定教材，Obsidian 是你的个人赛场经验库。每做一题，把“错误现象、原因、解决、下次避免方法”写回 Obsidian，下一次复习才会真正变快。
